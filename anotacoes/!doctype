ativa o modo (quirks = antigamente existia dois navegadores e antes era necessário identificar para qua navegador estamos programando). Se não for itencional não é recomendado criar um código sem o <!DOCTYPE html>